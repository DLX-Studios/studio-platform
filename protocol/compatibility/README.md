# Protocol compatibility

Compatibility adapters and version negotiation belong here. `studio-protocol` remains the only
authority for protocol-v1 types and validation; this directory must not define a competing schema.

# Protocol

Canonical contract layout for schemas and fixtures.
`crates/studio-protocol` remains the Rust type authority; this directory is the canonical generated
artifact surface. AssemblyScript bindings are generated in the SDK package because they are
language-specific SDK source, not protocol definitions.

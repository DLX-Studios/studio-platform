# studio-components

Target adapter layout for Longbridge `gpui-component`. The current `src/` implementation remains
the compatibility surface; the subdirectories classify adapters by lifecycle and capability.

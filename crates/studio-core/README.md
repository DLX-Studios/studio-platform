# studio-core

Target ownership boundary for protocol, retained UI registry, component runtime, event runtime,
and navigation runtime. Existing crates remain authoritative during migration:
`studio-protocol`, `studio-ui`, `studio-components`, and `studio-navigation`.
